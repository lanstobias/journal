#ifndef DATE
#define DATE

#include <ctime>
#include <locale>
#include <string>

namespace journal
{
    class Date
    {
    private:
        // Member variables

    public:
        // Contructors
        Date();

        // Accessors

        // Mutators

        // Public methods
        std::string getDateTime();

    private:
        // Private methods

    };
}
#endif
